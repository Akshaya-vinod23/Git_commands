git config --global user.name "Akshaya" >>> configur git with our name

git config --global user.email "akshayavonodkunnathu@gmailcom" >>> configur git with our email

configuration requires while pushing towards a public respository in github from aspository
git status >> used to check the status of .git in  the current directory

git init   >> to intialize git in the current directory (help to track the changes in our directory
if we add,delete a file it will track it as a change in the directory)

u >> represented untracked ( file is there not yetcommited or not added to staging area)

A >>file that added to the staging area

M >> file begin modified after committed

git add.intro.txt >>> intro.txt added to the staging area

git add.      >> all files added to the staging area

git commit -m "my first commit"
